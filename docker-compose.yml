version: "3"
services:
  app:
    build: ./
    command: npm run dev
    ports:
      - 5000:5000
    environment:
      PORT: 5000
    restart: always
    volumes:
      - ./:/src/
      - nodemodules:/src/node_modules

volumes:
  nodemodules: {}
